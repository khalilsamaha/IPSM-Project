import { ConfirmSubmitButton } from "@/components/confirm-submit-button";
import { archiveSeason, saveSeason } from "@/actions/records";
import { AppShell, Pager, SearchBox } from "@/components/records/shell";
import { requireSession } from "@/lib/auth/session";
import { prisma } from "@/lib/dashboard/data";
import { formatDate } from "@/lib/records/format";
import { buildPageHref, getPagination } from "@/lib/records/pagination";
export default async function SeasonsPage({ searchParams }: { searchParams: Promise<{ q?: string; page?: string }> }) {const session=await requireSession(); const {q,page,skip,take}=getPagination(await searchParams); const where=q?{name:{contains:q,mode:"insensitive" as const}}:{}; const [rows,total]=await Promise.all([prisma.season.findMany({where,orderBy:{startDate:"desc"},skip,take}),prisma.season.count({where})]); return <AppShell session={session}><h2 className="mb-4 text-2xl font-bold">Seasons</h2><SearchBox q={q} placeholder="Search seasons"/><form action={saveSeason} className="mb-6 grid gap-3 rounded-xl border bg-white p-4 md:grid-cols-4"><input name="name" required placeholder="Season name" className="rounded border p-2"/><input name="startDate" required type="date" className="rounded border p-2"/><input name="endDate" required type="date" className="rounded border p-2"/><select name="status" className="rounded border p-2"><option>PLANNED</option><option>ACTIVE</option><option>CLOSED</option><option>ARCHIVED</option></select><button className="rounded bg-primary px-4 py-2 text-white">Add season</button></form><div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-muted"><tr><th className="p-3">Name</th><th>Start</th><th>End</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.id}><td className="p-3 font-medium">{r.name}</td><td>{formatDate(r.startDate)}</td><td>{formatDate(r.endDate)}</td><td>{r.status}</td><td><form action={archiveSeason}><input type="hidden" name="id" value={r.id}/><ConfirmSubmitButton className="text-primary" message="Archive this record? This will hide it from active workflows.">Archive</ConfirmSubmitButton></form></td></tr>)}</tbody></table></div><Pager page={page} total={total} hrefFor={(p)=>buildPageHref('/seasons',p,q)}/></AppShell>}
